import { AnchorDirectiveDirective } from './anchor-directive.directive';

describe('AnchorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AnchorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
